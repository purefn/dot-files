* erase-your-darlings, sops, and home-manager are always loaded in basic profile. can they be in the conditional config?

* move home-manager modules into the profiles. maybe have profiles broken down into {profile}/nixos and {profile}/home-manager

* move profiles.nix to profiles/default.nix
